// Testing here has been spiked. Promise is received.

// function storesStoryObjects() {
//   storyhandler = new StoryHandler();
//   console.log(storyhandler.getStories());
//   assert.isTrue(storyhandler.getStories().length === 10);
// }

// storesStoryObjects();
